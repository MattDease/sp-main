#pragma strict

/***
    Game configuration.
    Set values through the editor in the GameManager prefab.
***/

// General debug mode of the game.
public var isDebug : boolean;
// Enable if connection test server is available.
public var netTestConnection : boolean;
